- var packageName = _.capitalize(package.name);
|// Router
|// Generated by #{packageName}
|// #{moment().format('MMMM Do YYYY, h:mm A')}

|
|module.exports = (router) => {
|
|   // Examples
|
|   // Bind HTTP verbs with paths and then to a controller and action
|   // router.get|put|post|patch|delete|options|head('/path').to('controllerName->actionName');
|
|   // Opens up
|   // GET|PUT|POST|PATCH|DELETE|OPTIONS|HEAD /path
|   // which gets handled by 'controllerName' controller
|   // and 'actionName' method inside of the controller
|
|   // To generate routes for a RESTful Resource
|   // router.resource('resourceNameInSungular');
|
|};